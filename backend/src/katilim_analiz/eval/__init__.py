"""Offline evaluation harnesses for the extraction layer.

Import the gold harness explicitly from ``katilim_analiz.eval.gold``; the
package intentionally re-exports nothing so that ``python -m
katilim_analiz.eval.gold`` does not double-import the module.
"""
